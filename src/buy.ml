(***)

(* Define a function within the UI module that calls make view_stock *)
(* let get_stock_price ticker = let result = Sys.command ("make view_stock " ^
   ticker) in if result = 0 then (* The command succeeded, so return the stock
   price *) let price = (* parse the output of the command to extract the price
   *) in Some price else (* The command failed, so return None *) None

   let execute_buy acct ticker num_shares = let price = get_stock_price ticker
   in a

   let update_bal acct *)
